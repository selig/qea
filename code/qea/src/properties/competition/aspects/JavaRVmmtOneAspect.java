package properties.competition.aspects;

/*
 * 
 * Base should implement check
 * 
public aspect JavaRVmmtOneAspect extends Base {

	pointcut step(int c) : within(rvcomp) && call(* step(int)) && args(c);
	
	private int STEP = 1;
	private final Monitor monitor;
	
	public JavaRVmmtOneAspect(){
		QEA qea = JavaRV_mmt.makeOne();
		monitor = MonitorFactory.make(qea);
	}
	
	before(int c) : step(c){
		check(monitor.step(STEP,c));
	}

}


*/
