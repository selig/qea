The run.sh script runs this complete example. It will download everything required so it is not necessary to checkout the qea repository. Just save the script locally and run it.

The Example.java file contains comments explaining how the tool is being used.
